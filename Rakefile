task :default do
  # system 'bundle exec compass compile --config _config/compass_config.rb'
  system 'bundle exec jekyll build --config _config/jekyll_config.yml'
end

task :serve do
  system 'bundle exec jekyll serve --config _config/jekyll_config.yml'
end
